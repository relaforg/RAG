[project]
name = "RAG"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fire>=0.7.1",
    "langchain-text-splitters>=1.1.1",
    "pydantic>=2.13.0",
]

[dependency-groups]
dev = [
    "flake8",
    "mypy",
]
